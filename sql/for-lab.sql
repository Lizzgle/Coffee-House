-- CALL AddNewUser('user4', 'password4', 'anna', '2000-01-01')

-- IN login_text TEXT,
--     IN password_text TEXT,
--     IN name_text TEXT,
--     IN dob DATE

-- INSERT INTO Desserts (Name, Calories) VALUES
--     ('candy potato', 200);

-- INSERT INTO OrdersDesserts (OrderId, DessertId) VALUES
--     (1, 6);

-- INSERT INTO RecipesIngredients (RecipeId, IngredientId) VALUES
--     (1, 1);
	
CALL AddItemToCart(1, 2, 2)

--  AddItemToCart(
--     IN cart_id INTEGER,
--     IN drink_id INTEGER,
--     IN dessert_id INTEGER
	
